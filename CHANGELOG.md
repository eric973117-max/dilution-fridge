# 改动记录

## 2026-09-20 · 真凶找到：≤480 的断点块把卡片底色又加回来了

- 现象（用户手机视口 ~430px）：卡片区域仍是深色面板；我此前在 824 / 882 宽验证都复现不出来。
- 原因：`@media (max-width: 480px)` 里还留着更早一版"给手机加厚玻璃"的两条规则
  （`.card { background: linear-gradient(...) }` / `.card.is-open { background: ... }`），
  它排在 ≤900 那套"去掉所有遮罩"**之后**，窄屏上把底色加了回来。
- 处理：整段删掉。现在手机端一律无底、只留文字（≤900 与 ≤480 都一致）。
- 验证：430×932 复现 → 修复后再读计算样式：`.card.is-on / .rail / .hud--tl / .dotgrid-host`
  全部 `background: none`、`backdrop-filter: none`；像素实测卡片区不再比周围暗。

## 2026-09-20 · 手机端取景改用"整机"包围盒：卡片后面不再有机器本体

- 现象：01–08 段里卡片区域看着是一块黑底。
- 排查：卡片与段位条的计算样式已是 `background: none` / `rgba(0,0,0,0)`；
  真正的原因是**取景只按"当前段"的包围盒算** —— 段盒只保证这一段装得下，
  机器上比它更靠下的零件（03 段的真空外罩、芯片、读出链）会漏到卡片后面，
  深色本体压在那里，看着就像卡片有底。
- 处理：手机端取景改用 `machine.bounds(null)`（**整机**包围盒）算 fit 与居中，
  段章与结尾章一律"整机装进带子"，卡片后面回到干净背景。

验证：882×1740（与你的窗口同尺寸）下 40 / 60 步截图。

## 2026-09-20 · 手机端 01–08 段：段位条与参数板也去掉遮罩

- 上一轮已经把章节抽屉 / 说明块 / 总表 / HUD 的底去掉；这一轮补上 01–08 段里剩下的两处：
  **段位条 01–08 背后的渐变**、**左上角参数板背后的径向暗底**。
- 文字改由更重的阴影兜底（参数板与段位条同样加 `text-shadow`）。

验证：824×1500 下 SEG 03（42 步）截图。

## 2026-09-20 · 手机端下半部分：取消所有遮罩，只留文字

- 章节抽屉、套罩层列表、总表、信号 / 剖面说明块、依赖图 —— 底部这些面板的
  底色、毛玻璃、scrim 全部去掉，可读性只靠文字阴影；模型因此完全透出来。
- 依赖图的节点框改成"只描边、不填底"，图例与文字同样无底。
- 总表八行比 UI 带还高、会顶进模型区 → 限高到带内并在带内滚动（带自己的滚动条）。

验证：390×844 下 32 / 124 / 150 / 158 四屏截图；桌面端未涉及。

## 2026-09-20 · 手机端：冷盘标签只留文字；最后两章可一键隐藏说明

- **冷盘标签**：去掉底色 / 边框 / 毛玻璃，只留一个小圆点 + 文字（加 `text-shadow`
  保证压在线稿上也读得清）—— 原来的深色小块会挡住机器。
- **最后两章（套罩 / 总表）加"隐藏说明"**：右上角一枚小胶囊按钮，只在手机端、只在这两章出现。
  点一下说明块淡出、`body` 带上 `ui-hidden`，`main.js` 里 UI 带随之归零 ——
  **模型立刻长满整屏**；再点恢复；离开这两章自动复位。

验证：390×844 下 32 步（标签无底）、150 步（显示 / 隐藏两态）截图。

## 2026-09-20 · Cryoperm 磁屏蔽加入逐层套罩；修好"落下"的偏移

- **问题**：磁屏蔽（Cryoperm）属于 08 段、当普通实体渲染，没进结尾那套逐层落位 ——
  所以看不到它的动画；层列表里也只有五层。
- **现在**：把它作为**最内一层**加进套罩序列（芯片级屏蔽，物理上本来也是第一个套上的）：
  - `data.js` 的 `SHIELD_LAYERS` 增加 `component` 字段 —— 辐射罩按挂在哪块板认领，
    真空外罩 / 磁屏蔽各只有一块、按组件认领；顺序为
    **01 Cryoperm → 02 MXC → 03 Still → 04 4 K → 05 50 K → 06 真空外罩**。
  - `dress()` 的罩子识别加上 `magnetic_`，它也走"深色玻璃 + 白线、落位转强调色"那一套；
    但它是 08 段自己的零件，不在结尾那叠里时也跟着 08 段显示（`layerComponent === 'magnetic'` 特例）。
  - 落位打标改成扫**所有段**（原来只扫 03 段），所以 08 段里的磁屏蔽也会淡入、下落、按层错开。
- **顺带修掉一个真 bug**：下落行程原来写成 `0.26 * LOCAL_PER_M`（= 260 m）——
  五层罩子其实只是淡入、没有"落下来"。改成按世界尺度换算到每件自己的局部空间
  （`0.26 / getWorldScale().y`），现在六层的下落都是真实的 26 cm。

验证：146 / 148 / 152 步截图 + CDP 探针（下落偏移 145.6mm → 0，六层都在）。

## 2026-09-20 · 手机端：抽屉按内容折叠 + 上滑/下滑手势 + 玻璃参数前后统一

- **修 bug**：收起态原来用 `min-height: 27vh` 撑高，在 824×1500 这种高窗口里比内容还高，
  点"展开"反而更矮、看不出在折叠什么。现在按**内容**折叠：收起只留「段号 + 简介」，
  展开才出参数表 / 缺失后果 / 片段（实测 166px ↔ 377px）。
- **手势**：卡片上部上滑展开、下滑收起（pointer 事件，触屏与鼠标拖拽都能用；
  滑动后抑制紧随的 click，避免连点两次）；把手旁加小字提示"上滑展开 / 下滑收起"。
- **玻璃统一**：`tokens.css` 立 `--glass-bg / --glass-bg-strong / --glass-blur`，
  手机端把这些面板全换成同一套 —— 章节抽屉、冷盘标签、左上参数板、说明块 scrim、依赖图节点条；
  之前"前段"（标签 / 参数板）比"后段"（说明块）更实，现在一致。

## 2026-09-20 · 手机端严格分区：模型只在上带、UI 只在下载

- 之前手机端 UI 会压住模型，卡片矮时下部又空着一片。
- `main.js` 的移动端取景改成按"带子"算：带子 = 顶部参数条(62px) 以下、UI 带以上；
  **UI 带高度取实际 UI 高度**（章节抽屉用 `offsetHeight`，结尾几章按 34vh 预留），
  底部再让开段位条 + 页脚(92px)。zoom 按带子高度算 fit，ty 把该段中心放进带子中心 ——
  卡片矮时模型自动长大补上空间，卡片再高也不会压住模型。
- 依赖图 / 规格表也挪进 UI 带（原先贴着顶部）；手机端"非当前段"压得更暗（0.16 → 0.07）。

## 2026-09-20 · 手机端抽屉改半透明，让模型透出来

- 手机端那张章节抽屉原来是近似不透明的黑底（rgba .88→.97），压住了机器下半身，
  模型信息被挡掉一大块。
- 现在：**收起态 = 半透明 + 毛玻璃**（`rgba(9,10,13,.52) → rgba(6,7,10,.74)` + `blur(14px)`）——
  底下的机器透得出来，文字靠模糊保持清晰；
  **展开态自动回到更实的底**（`.84 → .93` + `blur(18px)`），读长文不费劲；
  极窄屏（≤480px）再把玻璃加厚一档。
- 顺带：手机端冷盘标签从 128px 下移到 **186px**，让开左上角三行参数面板（原来两者叠着）。

验证：390×844 与 824×1500 两种窄窗各截一轮。桌面端未涉及。

## 2026-09-20 · 移动端（≤900px）：上模型 / 中段位条 / 下抽屉

上一版窄屏只是"把尺寸缩小"：卡片占掉下半屏、模型被挤扁、顶部参数面板还和按钮叠在一起。
这次按竖屏重排（桌面端一行未改）：

- **版面三段**：模型占上半屏；段位条 01–08 做成 44px 可点的一行，贴在抽屉下沿；
  章节卡改成**底部抽屉**（收起 27–34vh / 展开 68vh），上边缘加一个把手，点它展开收起
  （`ui.js` 加 `.card__handle` 并绑事件，桌面端把手隐藏）。
- **机位**：`main.js` 里 `innerWidth <= 900` 时 `zoom × 0.78`、`ty − 0.28`，
  把机器整体上移，让开抽屉与段位条。
- **冷盘标签**：手机上一次只讲一块 —— 3D 锚点挤在竖屏上读不出来，
  改成固定在左上角下方的一行（`plates.js` 的 mobile 分支），其余先不显示。
- **结尾几章**：这几章没有章节卡、抽屉那一条是空的 —— 信号 / 剖面 / 套罩的说明块改放进去
  （`padding-bottom: calc(dock + 6px)`），模型完整留在上半屏；
  套罩的五层压成两列、收起态只留编号 + 名字。
- **顶部**：参数面板压到三行、收窄到 56vw 并下移，让开右上角"跳过动画"按钮。
- 窄屏仍关掉引线（`.leaders { display: none }`），规格表走窄屏表格。

**待办（已确认）**：移动端最终要**另交付** —— 等这一版稳定后，用
`tools/build.mjs` 产出 `dist/web` 与 `dist/mobile` 两份产物，另建只放产物的交付仓
（或 `dist` 分支），两边用同一个 tag 对齐。这一步尚未开始。

验证：390×844（手机）/ 768×1024（平板）各截一轮；1600×1000 桌面端与改动前逐屏一致。

## 2026-09-20 · 套罩的配色改回"深色玻璃 + 白线"，落位那层用强调色

- 上一版为了去青，把罩子做成了"去色的浅灰"，结果又淡又闷、也不像这个站的东西。
- 现在按全站的线稿语言来：罩子本体是**深色半透明玻璃**（0x0d1014，白线描边），
  `dress()` 里给罩子单独一份线材质（不再蹭段里的 dim 线），套罩时按层亮起来；
  **正在落位的那一层轮廓转成强调色（珊瑚橙 #ff7d36）**，落定后回到白色 ——
  既显眼、也和整站的"黑底白线 + 珊瑚橙强调"统一。
- 配套：`shieldGlow` 1.7 → 1.25（深色本体不需要再提亮），罩子的轮廓线不再受
  "面数超 24000 就不描边"的限制。

## 2026-09-20 · 修：15–16 步冷盘标签叠在一起

- **现象**：那一轮讲到"混合室腔"时，展开的面板顶到上面的"冷板 CP"名牌，两块叠在一起。
- **原因**：名牌钉在盘沿上（盘与盘的间距是固定的），而展开态比收起态高约 4 倍，
  竖直方向一直没做避让 —— 靠下的那一块一展开就压到上一块。
- **处理**：`src/plates.js` 的 `update()` 加一次**竖直避让**：按每块的"顶部 / 底部"盒高
  从上往下排开，彼此留 10px 净空，再整体收进视口；展开态的说明高度上限同步从 130px 收到 104px，
  与避让用的 118px 估高一致。

## 2026-09-20 · 信号链路改成"沿真实那根微波线"走

- **先说明为什么之前不是"取模型里那根线"**：线体确实是用 `TubeGeometry` 建的，
  但模型最后跑了一遍 `optimize()`，把同材质的小网格合并成整批（`*_batch`），
  运行时已经取不到单独的 tube mesh 了 —— 所以按网格找中心线这条路走不通。
- **改成用模型自己的布线数据重建那根线**：`mechanical.rfPorts` 记着每一级上每个端口的
  `x / z / 衰减`，线体就是按「端口 + 两道 S 弯」每级 6 个控制点建的。现在照同一套规则，
  把第一路（sector 0 的 drive）在六级上接起来，末端接进芯片封装；
  曲线类型也用模型同款 `centripetal` —— 所以动画走的就是那根线本身。
- 结果：路径半径 ~173mm（端口实际位置，不是之前估算的 202mm）、y 从 +0.648 到 −0.509、
  32 个控制点；六个节点灯按算出的 u 值对齐 `[0, .194, .419, .806, .968, 1]`。

验证：截图 115 存于 docs/verify。

## 2026-09-20 · 让"超导传输段"看得见（06 章）

- 06 在这一版模型里**没有独立的几何块**：整串同轴线都属于 `wiring`（归 05），
  4 K 以下只是同一串线换了材料，所以那一章原先看不出"到底是哪一段"。
- 现在 06 章做三件事，把答案直接摆出来：
  - 焦点留在 05（**整串线束亮着**，其余段压暗）→ 亮着的那串线就是它讲的；
  - 把**整条信号路径**拉出来（沿真实线束半径），顺着线往下的那条蓝线就是它的走向；
  - **4 K 冷板**打上高亮环与名牌（`PLATE_CHAPTERS.seg06 = ['4k']`）→ 那条线就是分界：
    以上是 05 的输入配线，以下是 06 的超导同轴。

验证：截图 073 存于 docs/verify。

## 2026-09-20 · 罩子回到黑白；05/06 不再切几何；信号路径按真实线束走

### 1. 配色回到黑白（去掉浅青）

- 上一版给罩子按材质上色（铝 / 铜 / 不锈钢），结果带出一层浅青。现在只取原材质的**亮度**
  （去掉色相）再往中性深灰压一点 —— 罩子是灰白/深灰的，和整站的线稿白模一致。

### 2. 微波传输段：05 / 06 不再"分段"

- 上一版把 `wiring` 按 4 K 标高切成两半（上半 05、下半 06），动画里就出现
  "上下一半亮一半暗、动作各走各的"，看着凌乱。
- 现在**几何不切**：整串线束都留在 05 里一起动；06 那一章 `motion.focus = null`，
  不压暗其余段，只用机位 + 卡片讲"同一条线束的下半截（4 K → 10 mK 的超导同轴）"，
  引线仍指向 4 K 以下那段线束。

### 3. 信号链路改成沿真实微波线走

- 原来的链路是手摆的一条**贴着中轴**的曲线（x 从 0.166 收到 0），并不对应线束的实际位置。
- 现在按模型的真实走向生成：从室温顶法兰的 RF 馈通口出来，沿**线束挂载半径**
  （MXC 半径 250mm − 48mm ≈ 202mm）那一圈向下，逐级经过 50 K / 4 K / 100 mK / 10 mK，
  最后拐进芯片封装；六个节点就压在这几个路径点上，HUD 的节点灯按算出来的 t 对齐
  （`main.js` 启动时写进 `data-t`）。剖面章里看到的，就是这条线在机体里实际经过的位置。

验证：截图 062（05 章：整串线束）、070（06 章：不打压、只有引线指下半截）、
115（信号链路贴着线束半径走）、156（罩子灰白）存于 docs/verify。

## 2026-09-20 · 修：套罩时 MXC / Still 两层看不见

- **现象**：套罩章只有外侧几层罩子看得到，最内的 MXC 与 Still 两层像没上场。
- **原因**：两层罩子其实都建好、也都在渲染（每层 3 件、`visible = true`），但半透明罩子的
  不透明度只有约 0.10；而 MXC（半径 248mm）与 Still（298mm）本来就套在**机体内部**，
  在正常深度测试下被线束和平台整个挡在后面 —— 从外面根本看不到。
- **处理**：
  - 套罩那一段把罩子的 `depthTest` 关掉：内侧两层以半透明的方式压在机器上，看得见；
    离开这一段自动恢复（每帧只在状态真的变化时才改，不产生额外开销）。
  - 落位中的那一层加一个**亮度峰值**（f≈0.5 时最亮、落定后回落），"一层层套上"更清楚。
  - 罩子改回各自的材质色（铝 / 铜 / 不锈钢，掺一点冷色）：五层叠在一起时能靠色相分辨；
    套罩整体亮度 1.4 → 1.7。

验证：截图 147（第一层落位）/ 156（五层齐上）存于 docs/verify。

## 2026-09-20 · 依赖图回到方块面板；线束上下两截合成一个动作

### 1. 11 条依赖不再用连线画在机器上，回到之前那块 UI

- 恢复 `src/graph.js` 的 SVG 节点图（八段方块 + 四类关系连线，随滚动逐条描出）
  与 `playGraphIn / playGraphOut`；`machine/index.js` 里的 `depLines` 与 `setDeps` 删除。
- 版面重排：面板在**左侧、垂直居中**（宽度 `min(660px, 42vw)`），机器让到右半边并收小
  （`zoom 0.46 / tx -1.10`）—— 1268×828 与 1600×1000 两种窗口下都不裁切、不互相遮挡。

### 2. 信号线束不再"上下分家"

- 上一轮把 `wiring` 按 4 K 标高拆成两半（上半归 05、下半归 06），于是 05 章里
  下半截被压暗、动作也各走各的，看起来像被切开。
- 现在两截**仍分属 05 / 06**（06 那一章需要一个真实的挂点），但：
  - `main.js` 里 `detachWindow['06']` 直接复制 05 的窗口 —— 整串一起松、一起归位；
  - `motion.focus` 支持数组，讲 05 或 06 时两半一起亮（`machine.update` 里判断）。
- 结果：05/06 仍是用不同机位看同一串线的上下两段（和最早的做法一致），但动作与亮度是一体的。

### 验证

- `npm run check`：38 文件 / 89 引用。
- 截图：124（面板在左、机器在右，两种窗口各一张）、062（05 章：整串线一起亮）、
  070（06 章：同一条线束的下半截）。

## 2026-09-20 · 依赖关系画到机器上；05 / 06 两段分清

### 1. 依赖图换表现方式：不再用角落那块面板

- 11 条依赖直接画在机器上（`machine/index.js` 的 `depLines`）：按两端零件的**真实标高**连成弧线、
  按类型上色（承载 / 供冷 / 信号 / 屏蔽），随滚动逐条描出来（`drawRange`）——
  和信号链路同一套手法。
- 左下角只留一条图例 + 一句总结（`src/graph.js` 重写成图例；旧的 SVG 节点图、
  `playGraphIn/Out` 一并删掉）。
- 机位跟着回到"机器居中偏右"（`zoom 0.62 / tx -0.34`），不再为了给面板让位把机器推到角落。

### 2. 05 信号输入段 vs 06 超导传输段

两段本来是**同一串信号线的上下两截，4 K 是分界**；但模型里 `wiring` 整串都归在 05，
于是 06 一直是空段、文案也和 05 重叠。现在：

- `machine/index.js` 按标高把 4 K 冷板（180mm）以下的线束划给 06 ——
  06 有了自己的零件与挂点（`anchor` 也跟着改到 4 K 以下那段线束）。
- 文案各归各位：**05 = 300 K → 4 K 的输入配线**（分级衰减 ~20 dB、每级热锚）；
  **06 = 4 K → 10 mK 的超导同轴**（NbTi、损耗趋近 0、同轴屏蔽）。
- 依赖关系本身没变（05 → 06 → 07 → 08 这条信号链就是它们的分界依据）。

### 验证

- `npm run check`：38 文件 / 88 引用。
- 截图：062（05：上半截线束）、070（06：4 K 以下的线束 + 各自的说明）、
  124（依赖线画在机器上、左下只留图例）。

## 2026-09-20 · 分解改成合体的逆向、引线锚点重测、依赖图布局、套罩放慢并让位

### 1. 分解动画 = 合体动画的逆向

- 原来分解是"每章拆自己那一段"（01→08，和各章同序）。现在整机是**一起松、按 08→01 依次错开**
  地摊开的：窗口铺满八段章，段与段互相重叠，缓动与合体（`playReassembly`）共用 `machine.update`
  里同一条曲线 —— 看起来就是合体倒放。框架（01）最后才散，物理上也更顺。

### 2. 屏蔽罩不再提前出现（与结尾重复的内容取消）

- 罩子只出现在两处：**03 段那一章给最外的真空外罩**（这一章就是讲它），以及结尾**逐层套罩**。
- 剖面章与其余各章保持裸机；四级辐射罩留到结尾一层层套上时才出现。
- 03 段那章给真空外罩单独提亮一档（`shieldGlow 2.2`），免得"真空腔"淡到看不见。

### 3. 引线锚点按参数化模型实测重写

- `data.js` 里八段的 `anchor` 还是旧程序化模型的米制坐标，引线因此指错零件。
  现在按实测重写：01 平台外缘 / 02 右侧脉冲管冷头 / 03 真空外罩筒壁 / 04 中央稀释单元 /
  05 顶部线束树 / 06 4 K 以下同轴线束 / 07 MXC 下方读出链 / 08 芯片封装。
- 06 段在这一版模型里没有独立几何，`machine/index.js` 增加"跨段回退"：
  空段的锚点落到全机离它最近的真实零件上，引线不再指向空处。

### 4. 129 步（依赖图章）的版面

- 依赖图从右下角一小块挪到**左侧、放大**（`min(720px, 46vw)`，垂直居中），
  机器让到右半边并收小（`zoom 0.58→0.46`、`tx -1.45→-1.30`），整台散开的机器不再被切掉。

### 5. 套罩：放慢 + 不再压住模型

- 层列表从"底部居中一大片"改成**右侧竖排**（`styles/overlays.css` 的 `.overlay--shields`），
  机器相应左移（`tx +0.34`）并放大（`zoom 0.56`）；窄屏回到底部两列。
- 行程加长：`shields` 100→130vh（每层摊到约 26vh），错开窗口从 0.28 放到 0.42、
  下落行程 420/660→260/380mm，落位不再"啪"一下。总长用 `outro` 130vh 配平，
  前面各章步号保持不变。

### 验证

- `npm run check`：38 文件 / 89 引用。
- 截图：035（分解中 + 冷盘标签）、045（03 章只有真空外罩）、124（依赖图在左 / 机器在右）、
  140（定格）、152（套罩：列表在右、模型在左）、158（总表 + 八条引线各指其位）。

## 2026-09-20 · 步数刻度从 100 放到 160

- `src/data.js` 新增 `TOTAL_STEPS = 160`：HUD 的数字、以及"冷盘与腔室"那轮讲解的步窗都按它算，
  不再把总步数卡在 100。
- 结尾几段各留了更长的行程，动画不会被赶着播完：`hold` 51→85vh、
  `shields` 34→100vh（五层每层约 20vh）、`outro` 134→160vh。
- 冷盘/腔室通览跟着换到新刻度：`PLATE_TOUR` 5→19 步（8 个对象各约 1.75 步）。
- 新的落点：`assembly` 130–139 / `hold` 139–146 / `shields` 146–155 / `outro` 155–160；
  页面总长 1799 → 1925vh。

## 2026-09-19 · 冷盘/腔室挪到 3–9 步讲，套罩章瘦身，收尾拉近景

### 1. 第 3–9 步：冷盘与腔室逐个交待（那一带机位最适合看盘与腔）

- `src/data.js` 新增 `PLATE_TOUR`（from 3 / to 9，顺序 room → 50k → 4k → still → cold → mc →
  mxcvessel → vacuum）与 `CHAMBERS`（混合室腔、真空腔：位置与半径写死在模型坐标里）。
- 这 6 步里 8 个对象依次"亮起来"：名牌逐个出现（讲过的收成一行），**当前那一块**展开一句特点；
  3D 侧同步给这块盘换色 —— `machine/index.js` 给每块盘单独克隆了一份材质，
  高亮时往 `PLATE_COLORS` 的温度色偏（300 K 白 → 10 mK 深蓝，和页面温度色阶同一套），
  并在盘沿点一圈同色的环。腔体只用环 + 名牌（它们是空腔，没有实体可染色）。
- 分段章节里的标签保留，但改成"本段内轮着高亮"。

### 2. 套罩章瘦身

- 96–98 步只讲罩子：不再挂 8 段索引标注，也收起导航轨与总表（`ui.js` 的 `isAnnot` / `showRail`），
  版面只剩底部那五层列表。

### 3. 收尾拉近景

- 套罩章末尾（local > 0.72，约 97.4 步之后）机位从"装得下整机的远景"推到近景
  `SHIELDS_CLOSE`（`zoom 0.80 / el 8°`）；总表章从这近景接着走，模型比之前清楚得多。

### 验证

- `npm run check`：38 文件 / 89 引用。
- 分步截图：4.5 / 6.5 / 8.6（冷盘与腔室逐个交待、变色环）、97.5（套罩章只剩层列表）、
  99.5（近景 + 总表）。

## 2026-09-19 · 结尾改成「定格 → 逐层套罩 → 变色淡化」，前面补上冷盘标签

### 1. 03 段接回真实的屏蔽层

- 原来 `src/machine/index.js` 里 `SKIP = ['services','vacuum','radiation']`，把四级辐射罩和
  真空外罩整组丢掉了 —— 结果是 **03 段是个空壳**（`03:0`，讲到"真空与辐射屏蔽段"时场上没有零件）。
- 现在只跳过 `services`；罩子按 `src/data.js` 的 `SHIELD_LAYERS` 打上"第几层"的标，
  仍旧挂在 03 段里，只在讲屏蔽（03 章 / 剖面章）和结尾套罩时出现，其余章保持裸机。

### 2. 结尾重排：93–96 定格 / 96–98 逐层套罩 / 98–100 变色淡化

- `src/motion.js` 新增两章：`hold`（51vh，93.00→96.00%）与 `shields`（34vh，96.00→98.00%）；
  `outro` 保持 134vh（98.00→100%）。
  （HUD 的步是 `y ÷（总高 − 视口高）`，最后一章含一段滚不动的视口高度，所以 `vh` 这么配。）
- 定格章：相机姿势钉成常数，`clay 0.92 / bg 1 / dim 0.34` 全程不变。
  实测 93 / 94.5 / 95.9 三处相机位置、zoom、clay、dim 完全一致。
- 套罩章：`motion.shieldReveal` 由滚动驱动 0→1，五层按 `layer.at` 由内向外错开落位
  （MXC → Still → 4 K → 50 K → 真空外罩），还没轮到的层直接不画，落位时从上方降到位；
  底部 HUD 列出五层，当前那一层展开它的说明。
- 收尾章才把白模褪回线稿（变色淡化）并压暗。

### 3. 前面各章标出冷盘

- 新增 `src/plates.js`：六块平台各挂一个名牌（名字 / 英文名 / 温度 / 一句特点），
  锚点绑在最近的零件上，跟着分离、展开、翻面一起走（和 `leaders.js` 同一套做法）。
- 出现章节见 `PLATE_CHAPTERS`：seg01（室温法兰）、seg02/seg03（50 K、4 K）、
  seg04（Still、CP、MXC）、seg07（CP）、seg08（MXC）、xray（50 K→MXC）。
- 标签会自动躲到当前段落说明卡的对面一侧（卡片在右就往左挂），一次最多三块。

### 4. 顺带修正

- 03 段的规格行由"屏蔽层 2 级 / 材料 铝合金"改为"辐射罩 4 级 · 50K→MXC / 外罩 真空 · 不锈钢"，
  和实际模型一致。
- 定格 / 套罩章的总表抬头改用 `SEGMENT SPECIFICATION SHEET`（之前仍是 RE-ASSEMBLY）。

### 验证

- `npm run check`：38 个文件、89 条相对引用全部解析。
- 分步截图核对：22（预冷：50K / 4K 标签）、28（03 章：罩子到位、标签让到左侧）、
  35（稀释：Still / CP / MXC）、68（剖面）、93–95.9（定格，数值恒定）、
  96–98（逐层套罩）、99–100（变色淡化 + 总表）。
