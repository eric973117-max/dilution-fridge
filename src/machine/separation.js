/* ==========================================================================
   分离策略表
   段 id → 分离轴/距离/缩放/转角。
   —— 由 src/machine.js 拆分而来，几何与数值逻辑与拆分前逐字一致。
   ========================================================================== */



/* 轴向位移的幅度比上一版更大，并且侧向拉出的两段会同时转一点角度，
   让"它在动"这件事一眼能看出来 */
export const SEPARATION = {
  '02': { axis: 'y', dir: 1, dist: 0.38 },
  '03': { axis: 'y', dir: 1, dist: 0.46 },
  '04': { axis: 'y', dir: -1, dist: 0.30 },
  '05': { axis: 'radial', scale: 0.66 },
  '06': { axis: 'x', dir: 1, dist: 0.42, scale: 0.14, rotY: -14 },
  '07': { axis: 'x', dir: -1, dist: 0.42, scale: 0.14, rotY: 14 },
  '08': { axis: 'y', dir: -1, dist: 0.28 },
};
